#!/usr/bin/node
// Update this script to replace the value 12 with 89:
const myObject = {
    type: 'object',
    value: 12
  };
  console.log(myObject);
  // MY CODE HERE
  myObject.value = 89;
  // MY CODE UP THERE
  console.log(myObject);
